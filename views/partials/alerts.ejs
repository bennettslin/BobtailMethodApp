<% if (typeof alerts != 'undefined') { %>
  <% for (key in alerts) { %>
    <% alerts[key].forEach(function(message) { %>
      <div class="alert alert-<%= key %>"><%= message %></div>
    <% }); %>
  <% } %>
<% } %>