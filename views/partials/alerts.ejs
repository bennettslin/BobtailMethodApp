<% if (typeof alerts != 'undefined') { %>
  <% for (key in alerts) { %>
    <% alerts[key].forEach(function(message) { %>
      <div class="col-sm-6 col-sm-offset-3 alert alert-<%= key %>">
        <%= message %>
      </div>
    <% }); %>
  <% } %>
<% } %>